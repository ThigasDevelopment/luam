# Nosso runtime:
local text: string = 'Thigas';

function example (foo: string): void
	print (foo);
end

local object: any[] = {};
print (object.count); # 0

interface IPlayer {

}

class Player {
	constructor (): IPlayer {

		return self;
	}
}

# Buildado para o Lua 5.1 do MTA:
local text = 'Thigas';

function example (foo)
	print (foo);
end

local object = { };
print (table.size (object)); # 0

# Sistema de classes: github.com/ThigasDevelopment/lua-class (Refatorar algumas coisas dentro da Lib, como por exemplo `interface` nao tem mais pq existir se vai rodar Runtime)
class 'Player' {
	constructor = function (self)
		return self;
	end
}
