class PingCommand extends Command {
    name: string = 'ping'
    description: string = 'Writes a pong line into the debug console.'

    execute(player: any, ...): void {
        outputDebugString(formatPlayerLabel('pong'))
    }
}
