class PlayerJoinListener extends Listener {
    event: string = 'onPlayerJoin'

    handle = function (...): void
        local player: any = source

        outputChatBox(formatPlayerLabel(getPlayerName(player)) .. ' joined the server.', root)
    end
}
