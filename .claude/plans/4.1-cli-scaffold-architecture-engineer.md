---
id: 4.1
title: Scaffold CLI package and configuration
agent: architecture-engineer
status: done
depends-on: [3.4]
---

# 4.1 — Scaffold CLI package and configuration

> File: `.claude/plans/4.1-cli-scaffold-architecture-engineer.md`

## Objective

Create the `cli` package with a configuration schema for source dirs,
resource name, MTA server path, and RCON transport.

## Scope

- **In:** CLI package entry, config file schema, config loading/validation,
  command dispatch skeleton.
- **Out:** The actual `build`, `check`, and `ensure` behaviors.

## Steps

- [x] Create `packages/cli` with an executable entry point.
- [x] Define the project configuration schema.
- [x] Implement config loading and validation with diagnostics.
- [x] Add command dispatch for `build`, `check`, and `ensure`.

## Acceptance Criteria

- [x] `luam build` resolves and validates configuration.
- [x] Invalid configuration reports clear diagnostics.
- [x] Command dispatch routes `build`, `check`, and `ensure`.

## References

- Docs: [architecture](../docs/architecture.md), [tech-stack](../docs/tech-stack.md).
