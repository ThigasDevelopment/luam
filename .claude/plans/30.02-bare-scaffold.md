---
id: 30.02
title: Reduce `luam init` to the manifest and retire the framework
agent: architecture-engineer
status: done
depends-on: [06.03]
---

# 30.02 - Reduce `luam init` to the manifest and retire the framework

## Objective

Stop shipping a framework the language was never meant to own, so a build
contains exactly what the project authored.

## Delivered

`luam init` writes `luam.json` and nothing else. The `Core` / `Loader` / `Event`
/ `Listener` / `Command` / `ThreadPool` sketch moved to `examples/framework`,
alongside the old scaffold sources in `examples/resource`, and `@luam/template`
holds one file.

Nothing is injected into a resource beyond the runtime helpers an emitted
feature requires.

## Scope

- **In:** `luam init` output, `@luam/template`, the move of the framework
  sketch to `examples/`, and the external-reference record for a resolved
  ambient parent.
- **Out:** Re-analysing an open document when a later edit to another file adds
  or removes a declaration. The second pass covers workspace load, not live
  edits.

## Steps

### T-01 - Reduce the scaffold

**Agent:** architecture-engineer

- [x] Make `luam init` write `luam.json` and nothing else.
- [x] Move the framework sketch to `examples/framework` and the old scaffold
      sources to `examples/resource`.
- [x] Reduce `@luam/template` to a single file.

### T-02 - Correct cross-file class resolution

**Agent:** architecture-engineer

- [x] Record a resolved ambient parent as an external reference.
- [x] Pass ambient declarations from the LSP to the checker, with a second
      `load` pass so a document sees declarations of files scanned after it.

## Acceptance Criteria

- [x] `luam init` writes `luam.json` and nothing else. No framework, no example
      sources, no directory tree the user has to delete before writing their own
      first file.
- [x] A build ships exactly what the project authored. Nothing is injected into
      the resource beyond the runtime helpers an emitted feature requires.
- [x] `@luam/template` holds one file. The `Core` / `Loader` / `Event` /
      `Listener` / `Command` / `ThreadPool` sketch moved to `examples/framework`,
      alongside the old scaffold sources in `examples/resource`.

## Decided during implementation

- **The framework is not part of the language.** It was written as reference for
  how MTA class-based resources structure themselves, and milestone 6 mistook
  reference material for a deliverable. Neither scaffolding it nor injecting it
  is the language's job; `examples/` is where it belongs.
- **A resolved ambient parent is now an external reference.** `resolveSuperClass`
  only noted one when the parent was *missing*, so a module that extends a class
  from another file reported borrowing nothing. The record was simply wrong.
- **The LSP passes ambient declarations to the checker,** and `load` runs a
  second pass so a document sees the declarations of files scanned after it.
  Before this, `class Derived extends Base` was reported `check-unknown-class` in
  the editor whenever `Base` lived in another file — the checker had never been
  given anything but the open document. `luam check` was unaffected, which is why
  the suite never caught it.

## References

- [roadmap](../docs/roadmap.md) — Milestone 30
- [project](../docs/project.md)
