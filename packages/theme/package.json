{
    "name": "@luam/theme",
    "version": "0.19.2",
    "private": true,
    "type": "module",
    "main": "./src/resolve.ts",
    "types": "./src/resolve.ts",
    "files": [
        "dist-themes",
        "src"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "themes": "node scripts/build-theme.ts",
        "themes:check": "node scripts/build-theme.ts --check",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json"
    },
    "devDependencies": {
        "@types/node": "^22.9.0",
        "esbuild": "^0.21.5",
        "typescript": "^5.6.3",
        "vitest": "^2.1.4"
    }
}
