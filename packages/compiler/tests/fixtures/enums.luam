enum GameState {
    LOBBY,
    PLAYING,
    PAUSED,
}

enum UnusedState {
    NONE,
}

local enum Weather {
    CLEAR,
    RAIN,
}

local state: number = GameState.PLAYING
local sky: number = Weather.RAIN

if state == GameState.LOBBY then
    print('waiting for players')
end
