{
    "name": "@luam/compiler",
    "version": "1.0.10",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "test": "vitest run",
        "test:bench": "vitest bench --run",
        "typecheck": "tsc -p tsconfig.json"
    },
    "dependencies": {
        "@luam/mta-types": "workspace:*",
        "@luam/runtime": "workspace:*"
    },
    "devDependencies": {
        "@types/node": "^22.9.0",
        "fengari": "^0.1.5",
        "typescript": "^5.6.3",
        "vitest": "^2.1.4"
    }
}
