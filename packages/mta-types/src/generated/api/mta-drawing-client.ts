import type { ApiCatalog } from '@mta-types/api-declaration';
import { ANY, BOOLEAN, fn, named, NUMBER, STRING, TABLE, tupleOf } from '@mta-types/type-descriptor';

export const MTA_DRAWING_CLIENT: ApiCatalog = {
    dxConvertPixels: fn([STRING, STRING, NUMBER], STRING, 2),
    dxCreateFont: fn([STRING, NUMBER, BOOLEAN, STRING], named('Element'), 1),
    dxCreateRenderTarget: fn([NUMBER, NUMBER, BOOLEAN], named('Element'), 2),
    dxCreateScreenSource: fn([NUMBER, NUMBER], named('Element'), 2),
    dxCreateShader: fn([STRING, NUMBER, NUMBER, BOOLEAN, STRING], tupleOf([named('Element'), STRING]), 1),
    dxCreateTexture: fn([ANY, ANY, ANY, STRING, ANY, ANY], named('DxTexture'), 1),
    dxDrawCircle: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN], BOOLEAN, 3),
    dxDrawImage: fn([NUMBER, NUMBER, NUMBER, NUMBER, ANY, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN], BOOLEAN, 5),
    dxDrawImageSection: fn(
        [NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, ANY, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN],
        BOOLEAN,
        9,
    ),
    dxDrawLine: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN], BOOLEAN, 5),
    dxDrawLine3D: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN], BOOLEAN, 6),
    dxDrawMaterialLine3D: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, ANY, ANY, NUMBER, ANY, ANY, NUMBER, NUMBER, ANY], BOOLEAN, 8),
    dxDrawMaterialPrimitive: fn([ANY, ANY, BOOLEAN, TABLE, TABLE], BOOLEAN, 4, true),
    dxDrawMaterialPrimitive3D: fn([ANY, ANY, BOOLEAN, TABLE, TABLE], BOOLEAN, 4, true),
    dxDrawMaterialSectionLine3D: fn(
        [NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, ANY, ANY, NUMBER, ANY, ANY, NUMBER, NUMBER, ANY],
        BOOLEAN,
        12,
    ),
    dxDrawPrimitive: fn([STRING, BOOLEAN, TABLE, TABLE], BOOLEAN, 3, true),
    dxDrawPrimitive3D: fn([STRING, BOOLEAN, TABLE, TABLE, TABLE, TABLE], BOOLEAN, 5, true),
    dxDrawRectangle: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN, BOOLEAN], BOOLEAN, 4),
    dxDrawText: fn(
        [
            STRING,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            ANY,
            STRING,
            STRING,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
        ],
        BOOLEAN,
        3,
    ),
    dxDrawWiredSphere: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER], BOOLEAN, 7),
    dxGetBlendMode: fn([], STRING, 0),
    dxGetFontHeight: fn([NUMBER, ANY], NUMBER, 0),
    dxGetMaterialSize: fn([named('Element')], tupleOf([NUMBER, NUMBER, ANY]), 1),
    dxGetPixelColor: fn([STRING, NUMBER, NUMBER], tupleOf([NUMBER, NUMBER, NUMBER, NUMBER]), 3),
    dxGetPixelsFormat: fn([STRING], STRING, 1),
    dxGetPixelsSize: fn([STRING], tupleOf([NUMBER, NUMBER]), 1),
    dxGetStatus: fn([], TABLE, 0),
    dxGetTextSize: fn([STRING, NUMBER, NUMBER, NUMBER, ANY, BOOLEAN, BOOLEAN], tupleOf([NUMBER, NUMBER]), 1),
    dxGetTexturePixels: fn([ANY, ANY, NUMBER, NUMBER, NUMBER, ANY], STRING, 1),
    dxGetTextWidth: fn([STRING, NUMBER, ANY, BOOLEAN], NUMBER, 1),
    dxIsAspectRatioAdjustmentEnabled: fn([], tupleOf([BOOLEAN, NUMBER]), 0),
    dxSetAspectRatioAdjustmentEnabled: fn([BOOLEAN, NUMBER], BOOLEAN, 1),
    dxSetBlendMode: fn([STRING], BOOLEAN, 1),
    dxSetPixelColor: fn([STRING, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER], BOOLEAN, 6),
    dxSetRenderTarget: fn([named('Element'), BOOLEAN], BOOLEAN, 0),
    dxSetShaderTessellation: fn([named('Element'), NUMBER, NUMBER], BOOLEAN, 3),
    dxSetShaderTransform: fn([named('Element'), NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN, NUMBER, NUMBER, BOOLEAN], BOOLEAN, 4),
    dxSetShaderValue: fn([named('Element'), STRING, ANY], BOOLEAN, 3),
    dxSetTestMode: fn([STRING], BOOLEAN, 1),
    dxSetTextureEdge: fn([named('DxTexture'), STRING, NUMBER], BOOLEAN, 2),
    dxSetTexturePixels: fn([ANY, ANY, ANY, NUMBER, NUMBER, NUMBER, ANY], BOOLEAN, 2),
    dxUpdateScreenSource: fn([named('Element'), BOOLEAN], BOOLEAN, 1),
};
