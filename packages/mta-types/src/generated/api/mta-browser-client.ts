import type { ApiCatalog } from '@mta-types/api-declaration';
import { ANY, BOOLEAN, fn, named, NUMBER, STRING, TABLE } from '@mta-types/type-descriptor';

export const MTA_BROWSER_CLIENT: ApiCatalog = {
    canBrowserNavigateBack: fn([named('Browser')], BOOLEAN, 1),
    canBrowserNavigateForward: fn([named('Browser')], BOOLEAN, 1),
    createBrowser: fn([NUMBER, NUMBER, BOOLEAN, BOOLEAN], named('Browser'), 3),
    executeBrowserJavascript: fn([named('Browser'), STRING], BOOLEAN, 2),
    focusBrowser: fn([named('Browser')], BOOLEAN, 1),
    getBrowserProperty: fn([named('Browser'), STRING], BOOLEAN, 2),
    getBrowserSettings: fn([], TABLE, 0),
    getBrowserSource: fn([named('Browser'), fn([], ANY, 0, true, undefined)], BOOLEAN, 2),
    getBrowserTitle: fn([named('Browser')], STRING, 1),
    getBrowserURL: fn([named('Browser')], STRING, 1),
    injectBrowserMouseDown: fn([named('Browser'), STRING, BOOLEAN], BOOLEAN, 2),
    injectBrowserMouseMove: fn([named('Browser'), NUMBER, NUMBER], BOOLEAN, 3),
    injectBrowserMouseUp: fn([named('Browser'), STRING], BOOLEAN, 2),
    injectBrowserMouseWheel: fn([named('Browser'), NUMBER, NUMBER], BOOLEAN, 3),
    isBrowserDomainBlocked: fn([STRING, BOOLEAN], BOOLEAN, 1),
    isBrowserFocused: fn([named('Browser')], BOOLEAN, 1),
    isBrowserGPUEnabled: fn([], BOOLEAN, 0),
    isBrowserLoading: fn([named('Browser')], BOOLEAN, 1),
    isBrowserRenderingPaused: fn([named('Browser')], BOOLEAN, 1),
    loadBrowserURL: fn([named('Browser'), STRING, STRING, BOOLEAN], BOOLEAN, 2),
    navigateBrowserBack: fn([named('Browser')], BOOLEAN, 1),
    navigateBrowserForward: fn([named('Browser')], BOOLEAN, 1),
    reloadBrowserPage: fn([named('Browser'), BOOLEAN], BOOLEAN, 1),
    requestBrowserDomains: fn([TABLE, BOOLEAN, fn([], ANY, 0, true, undefined)], BOOLEAN, 1),
    resizeBrowser: fn([named('Browser'), NUMBER, NUMBER], BOOLEAN, 3),
    setBrowserAjaxHandler: fn([named('Browser'), STRING, fn([], ANY, 0, true, undefined)], BOOLEAN, 2),
    setBrowserProperty: fn([named('Browser'), STRING, STRING], BOOLEAN, 3),
    setBrowserRenderingPaused: fn([named('Browser'), BOOLEAN], BOOLEAN, 2),
    setBrowserVolume: fn([named('Browser'), NUMBER], BOOLEAN, 1),
    toggleBrowserDevTools: fn([named('Browser'), BOOLEAN], BOOLEAN, 2),
};
