import type { ApiCatalog } from '@mta-types/api-declaration';
import { ANY, BOOLEAN, fn, named, NUMBER, STRING, tupleOf } from '@mta-types/type-descriptor';

export const MTA_WORLD_CLIENT: ApiCatalog = {
    createSWATRope: fn([NUMBER, NUMBER, NUMBER, NUMBER], BOOLEAN, 4),
    getBirdsEnabled: fn([], BOOLEAN, 0),
    getCoronaReflectionsEnabled: fn([], NUMBER, 0),
    getGarageBoundingBox: fn([NUMBER], tupleOf([NUMBER, NUMBER, NUMBER, NUMBER]), 1),
    getGaragePosition: fn([NUMBER], tupleOf([NUMBER, NUMBER, NUMBER]), 1),
    getGarageSize: fn([NUMBER], tupleOf([NUMBER, NUMBER, NUMBER]), 1),
    getGroundPosition: fn([NUMBER, NUMBER, NUMBER], NUMBER, 3),
    getInteriorFurnitureEnabled: fn([NUMBER], BOOLEAN, 1),
    getInteriorSoundsEnabled: fn([], BOOLEAN, 0),
    getNearClipDistance: fn([], NUMBER, 0),
    getPedsLODDistance: fn([], NUMBER, 0),
    getRoofPosition: fn([NUMBER, NUMBER, NUMBER], NUMBER, 3),
    getScreenFromWorldPosition: fn([NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN], tupleOf([NUMBER, NUMBER, NUMBER]), 3),
    getVehiclesLODDistance: fn([], tupleOf([NUMBER, NUMBER]), 0),
    getWorldFromScreenPosition: fn([NUMBER, NUMBER, NUMBER], tupleOf([NUMBER, NUMBER, NUMBER]), 3),
    isAmbientSoundEnabled: fn([STRING], BOOLEAN, 1),
    isLineOfSightClear: fn(
        [NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, BOOLEAN, BOOLEAN, BOOLEAN, BOOLEAN, BOOLEAN, BOOLEAN, BOOLEAN, named('Element')],
        BOOLEAN,
        6,
    ),
    isWorldSoundEnabled: fn([NUMBER, NUMBER], BOOLEAN, 1),
    isWorldSpecialPropertyEnabled: fn([STRING], BOOLEAN, 1),
    processLineOfSight: fn(
        [
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            BOOLEAN,
            named('Element'),
            BOOLEAN,
            BOOLEAN,
        ],
        tupleOf([
            BOOLEAN,
            NUMBER,
            NUMBER,
            NUMBER,
            named('Element'),
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
            NUMBER,
        ]),
        6,
    ),
    resetAmbientSounds: fn([], BOOLEAN, 0),
    resetBlurLevel: fn([], BOOLEAN, 0),
    resetColorFilter: fn([], BOOLEAN, 0),
    resetCoronaReflectionsEnabled: fn([], BOOLEAN, 0),
    resetNearClipDistance: fn([], BOOLEAN, 0),
    resetPedsLODDistance: fn([], BOOLEAN, 0),
    resetVehiclesLODDistance: fn([], BOOLEAN, 0),
    resetWorldSounds: fn([], BOOLEAN, 0),
    setAmbientSoundEnabled: fn([STRING, BOOLEAN], BOOLEAN, 2),
    setBirdsEnabled: fn([BOOLEAN], BOOLEAN, 1),
    setColorFilter: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER], BOOLEAN, 8),
    setCoronaReflectionsEnabled: fn([NUMBER], BOOLEAN, 1),
    setInteriorFurnitureEnabled: fn([NUMBER, BOOLEAN], BOOLEAN, 2),
    setNearClipDistance: fn([NUMBER], BOOLEAN, 1),
    setPedsLODDistance: fn([NUMBER], BOOLEAN, 1),
    setVehiclesLODDistance: fn([NUMBER, NUMBER], BOOLEAN, 1),
    setWorldSoundEnabled: fn([NUMBER, ANY, BOOLEAN, ANY], BOOLEAN, 2),
    setWorldSpecialPropertyEnabled: fn([STRING, BOOLEAN], BOOLEAN, 2),
    testLineAgainstWater: fn([NUMBER, NUMBER, NUMBER, NUMBER, NUMBER, NUMBER], tupleOf([BOOLEAN, NUMBER, NUMBER, NUMBER]), 6),
};
