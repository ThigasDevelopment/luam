class Adapter {
    connection?: userdata

    use = function (): void
        local ready = self.connection ~= nil

        if ready then
            local handle: userdata = self.connection

            outputDebugString(tostring(handle))
        end
    end
}
